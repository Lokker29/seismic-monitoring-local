# seismic-monitoring-local

### Requirements
- `python3.6`
- `pip`

You need make few steps:
- create file **config.py** like **config-example.txt** in root directory
- run ```pip install -r requirements.txt```

### Start
- run ```make```
